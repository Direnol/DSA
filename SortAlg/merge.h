#ifndef MERGE_H
#define MERGE_H

#include <stdlib.h>

void merge(int *array, int *left_array, int left_count, int *right_array,
           int right_count);
void mergeSort(int *array, int size);

#endif
